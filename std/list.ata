fun isEmptyList [a] -- Bool { [] = }
fun head        [a] -- a    { !. swap drop }
fun tail        [a] -- [a]  { !. drop }

fun __len__ [a] -- [a], Int {
    dup isEmptyList if {
        0
    } else {
        tail __len__ 1 +
    }
}

fun len [a] -- Int {
    __len__ swap drop
}