func hello -- string bool : () {
    if { "!" } else { "" }
    "Hello " puts swap puts putsln
}

func prompt -- string : string {
    puts flush gets
}

; main entry point

entry {
    "What is your name? " prompt
    #true hello
}