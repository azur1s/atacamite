: fizzbuzz (Int -> [Char])
    dup 15 % 0 = if
        "FizzBuzz"
    else
        dup 3 % 0 = if
            "Fizz"
        else
            dup 5 % 0 = if
                "Buzz"
            else
                dup str
            end
        end
    end
;

: main ()
    15 fizzbuzz putsln
;